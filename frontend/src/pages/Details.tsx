import ContentList from "../components/ContentList";
import CommentList from "../components/comments/CommentList";
// user, ticket and project page
// if ticket details listed, then mount comment list

const Details = () => {
  return (
    <div>Details</div>
  )
}
export default Details;